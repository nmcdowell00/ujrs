---
type Props = {
  name: string;
  date: string;
  time: string;
  description: string;
};

const { name, date, time, description } = Astro.props;
---

<div class="event-card border border-gray-300 bg-white shadow-sm overflow-hidden mx-auto mb-4">
  <div class="event-content p-4">
    <!-- Event Name -->
    <h2 class="event-name text-2xl text-black mb-2">{name}</h2>

    <!-- Event Date and Time -->
    <div class="event-details text-sm text-gray-600 mb-4">
      <p><span class="label font-medium text-gray-800">Date:</span> {date}</p>
      <p><span class="label font-medium text-gray-800">Time:</span> {time}</p>
    </div>

    <!-- Event Description -->
    <p class="event-description text-sm text-gray-700">{description}</p>
  </div>

  <!-- Call to Action or Footer (optional) -->
  <div class="event-footer bg-gray-100 p-2 text-right">
    <button class="bg-teal-700 text-white px-4 py-1 rounded hover:bg-teal-600 transition duration-200">
      Sign up
    </button>
  </div>
</div>
